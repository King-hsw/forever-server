<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.forever.server.auth.SysAuthTokenMapper">

    <insert id="insert">
        INSERT INTO sys_auth_token (user_id, access_token, refresh_token, access_expires_at, refresh_expires_at)
        VALUES (#{userId}, #{accessToken}, #{refreshToken}, #{accessExpiresAt}, #{refreshExpiresAt})
    </insert>

    <select id="findByAccessToken" resultType="com.forever.server.auth.SysAuthToken">
        SELECT * FROM sys_auth_token WHERE access_token = #{accessToken}
    </select>

    <select id="findByRefreshToken" resultType="com.forever.server.auth.SysAuthToken">
        SELECT * FROM sys_auth_token WHERE refresh_token = #{refreshToken}
    </select>

    <delete id="deleteById">
        DELETE FROM sys_auth_token WHERE id = #{id}
    </delete>

    <delete id="deleteByRefreshToken">
        DELETE FROM sys_auth_token WHERE refresh_token = #{refreshToken}
    </delete>

    <delete id="purgeExpired">
        DELETE FROM sys_auth_token WHERE refresh_expires_at &lt; CURRENT_TIMESTAMP
    </delete>
</mapper>
