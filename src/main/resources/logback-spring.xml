<?xml version="1.0" encoding="UTF-8"?>
<!--
  日志配置：
  - 控制台始终输出（开发主要看这里）
  - 文件输出到 logs/forever-server.log，按天 + 大小滚动，保留 14 天
    （路径可用 logging.file.name 覆盖，见 application-prod.yml）
-->
<configuration>
    <!-- 从 Spring 环境读取日志文件路径，默认项目根目录 logs/ -->
    <springProperty scope="context" name="LOG_FILE" source="logging.file.name"
                    defaultValue="logs/forever-server.log"/>

    <!-- 彩色控制台格式（开发用）：时间 级别 线程 [traceId占位] logger - 消息 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %highlight(%-5level) %boldMagenta(%-12thread) %cyan(%logger{36}) - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 滚动文件：每天一个新文件；单文件超 20MB 也滚动；历史保留 14 天，总量上限 1GB -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>20MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 开发环境：本项目代码开 DEBUG，看 MyBatis SQL 就够用了 -->
    <springProfile name="dev | default">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.forever.server" level="DEBUG"/>
    </springProfile>

    <!-- 生产环境：控制台仅 WARN 以上，文件 INFO 以上 -->
    <springProfile name="prod">
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        <logger name="com.forever.server" level="INFO"/>
    </springProfile>
</configuration>
